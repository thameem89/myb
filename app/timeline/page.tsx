import { PageHero } from "@/components/site-shell"; import { statusTone, timeline } from "@/lib/content";
export const metadata={title:"Documented Timeline",description:"A chronological, source-labelled view of the featured customer experience."};
export default function Page(){return <><PageHero eyebrow="Chronology & sources" title="A timeline that separates record from recollection." intro="Each entry is labelled by source and evidence status. Dates remain withheld until reviewed supporting records are cleared for publication."/><section className="content-section"><div className="container timeline">{timeline.map(({date,title,text,type,icon:Icon})=><article className="timeline-item" key={title}><span className="timeline-date">{date}</span><span className="timeline-icon"><Icon/></span><div className="timeline-card"><span className={`label ${statusTone[type]}`}>{type}</span><h3>{title}</h3><p>{text}</p></div></article>)}</div></section></>}
