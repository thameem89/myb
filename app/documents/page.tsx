import { PageHero } from "@/components/site-shell"; import { FileText, LockKeyhole } from "lucide-react";
export const metadata={title:"Redacted Document Library"};
const docs=[["Initial payment receipt","Payment confirmation","Public placeholder · redacted version pending"],["License-related email","Email","Private original retained · public review pending"],["Refund communication","Refund communication","Public placeholder · source review pending"]];
export default function Page(){return <><PageHero eyebrow="Privacy-first evidence library" title="Documents, with context and careful redaction." intro="Only reviewed, redacted public copies appear here. Private originals are never exposed through public links."/><section className="content-section"><div className="container"><div className="legal-note"><LockKeyhole size={16}/> Sample placeholders only — no evidence, dates or documents have been fabricated.</div><div className="card-grid">{docs.map(([t,c,s])=><article className="info-card" key={t}><FileText/><span className="eyebrow">{c}</span><h3>{t}</h3><p>{s}</p><span className="pill grey">Awaiting evidence</span></article>)}</div></div></section></>}
