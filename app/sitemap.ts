import type { MetadataRoute } from "next"; import { guides, policies } from "@/lib/content";
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL||"https://clearterms.example";const pages=["","our-experience","timeline","documents","reviews","guides","submit-experience","company-response","about","contact"];const slug=(v:string)=>v.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");return [...pages.map(p=>({url:`${base}/${p}`,changeFrequency:"monthly" as const,priority:p===""?1:.7})),...guides.map(([t])=>({url:`${base}/guides/${slug(t)}`,changeFrequency:"monthly" as const,priority:.6})),...policies.map(([,p])=>({url:`${base}/policies/${p}`,changeFrequency:"yearly" as const,priority:.3}))]}
