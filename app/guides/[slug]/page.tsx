import Link from "next/link"; import { notFound } from "next/navigation"; import { guides } from "@/lib/content";
const slugify=(v:string)=>v.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/(^-|-$)/g,"");
export function generateStaticParams(){return guides.map(([t])=>({slug:slugify(t)}))}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const guide=guides.find(([t])=>slugify(t)===slug);if(!guide)notFound();const [title,category,time]=guide;return <><section className="page-hero"><div className="container narrow"><span className="eyebrow">{category} · {time} read</span><h1>{title}</h1><p className="lede">A practical, general-information guide for documenting expectations and reducing avoidable disputes.</p></div></section><section className="content-section"><article className="container prose"><div className="legal-note">Sample editorial content. Review and localize with a qualified UAE professional before publication. This is not legal advice.</div><h2>Start with clarity</h2><p>Before appointing a service provider, identify exactly what is included, who is responsible for each step, what the expected timeline is and what happens when circumstances change.</p><h2>Questions to put in writing</h2><ul><li>What services and government fees are included?</li><li>What payment dates, refund conditions and deductions apply?</li><li>Who can submit, amend or cancel an application?</li><li>How will material changes be notified?</li><li>Which records will you receive and retain?</li></ul><h2>Keep a reliable record</h2><p>After a call or meeting, send a short email summarising what you understood. Ask the other party to confirm or correct it. Store agreements, receipts, messages and application records together.</p><h2>If a disagreement arises</h2><p>Use dates and documents. Separate what you personally observed from what you inferred. State the resolution you want, allow a reasonable response period and protect private personal data in anything shared publicly.</p><Link className="text-link" href="/guides">Back to all awareness guides</Link></article></section></>}
